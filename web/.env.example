# Frontend environment variables (copy to web/.env.local for local development)
# Vite exposes only variables prefixed with VITE_

# Public OAuth client IDs — safe to embed in the frontend bundle
VITE_GOOGLE_CLIENT_ID=
VITE_FACEBOOK_APP_ID=
